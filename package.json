{
  "name": "atmosphera",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack weather application — PM Accelerator Technical Assessment",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "client": "npm run dev --prefix client",
    "server": "npm run dev --prefix server",
    "build": "npm run build --prefix client",
    "install:all": "npm install && npm install --prefix client && npm install --prefix server && npx --prefix server prisma generate"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
